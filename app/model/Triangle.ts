export class Triangle {
  height: number;
  width: number;
  area: number;

  constructor(height: number, width: number, area: number) {
    this.height = height;
    this.width = width;
    this.area = area;
  }
}
